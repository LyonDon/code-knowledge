定义了一个算法的步骤，并允许子类为一个或者多个子类提供实现

方法内部的部分子方法是由本方法实现的，另一部分子方法是由子类实现的

在一个方法中定义了一个算法的骨架，而将一些步骤延迟到子类中。使得子类可以在不改变算法结构的情况下，重新定义算法中的某些步骤

模板方法应被声明为final，防止子类的对其构造的改变

挂钩（hook）方法会为模板方法提供一个默认的子方法，并添加条件语句，是否实现这个子方法由子类自己来决定
